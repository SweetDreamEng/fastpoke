import React from "react";

const Banner = () => {
    return (
        <section className="banner content subheader">
            <div className="container">
                <h1>The right opportunity is just around the corner.</h1>
            </div>
        </section>
    );
};

export default Banner;